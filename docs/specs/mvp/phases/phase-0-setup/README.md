---
title: Phase 0 — Setup
artifact_type: phase-readme
status: approved
version: 1.0
phase_number: 0
phase_name: Setup
created: 2026-04-22
updated: 2026-04-22
owner: engineering lead
req_ids: []
delta:
  new:
    - Phase 0 README (example of phase folder structure)
  changed: []
  impacted: []
  unchanged: []
governing_rules:
  - Constitution R17, R19
  - PRD §14 (Timeline), §6.5 (Project Structure)
---

# Phase 0 — Setup

> **Summary (~150 tokens):** Monorepo + Docker + Postgres + TypeScript + CLI skeleton. Goal: `pnpm cro:audit --version` runs; Postgres is up; Drizzle migrations applied. Blocks all other phases. 3 days. 5 tasks (M0.1-M0.5). No external risk — pure setup.

## Goal

`pnpm cro:audit --version` prints a version; local Postgres 16 + pgvector is running; `pnpm db:migrate` applies migrations cleanly; TypeScript builds across workspaces.

## Tasks

| Task | Description |
|---|---|
| M0.1 | Initialize monorepo (pnpm + Turborepo + root package.json) |
| M0.2 | Create `packages/agent-core` skeleton (tsconfig, Vitest) |
| M0.3 | Create `apps/cli` skeleton |
| M0.4 | Docker Compose for Postgres 16 + pgvector |
| M0.5 | `.env.example` with all required keys |

Full task descriptions: `tasks.md` in this folder (generated by Spec Kit CLI from `PRD.md` F-001). Cross-reference: `docs/specs/mvp/tasks-v2.md` T001-T005.

## Exit criteria

- [ ] `pnpm install` succeeds at repo root
- [ ] `pnpm build` compiles all packages
- [ ] `pnpm cro:audit --version` prints version
- [ ] `docker-compose up -d` starts Postgres
- [ ] `pnpm db:migrate` creates initial schema

## Depends on

**Nothing.** Start here.

## Blocks

- Phase 0b (Heuristic Authoring) — can start in parallel, technically does NOT depend on Phase 0 engineering work but does benefit from the `heuristics-repo/` folder being created
- Phase 1 (Browser Perception Foundation) — depends on workspace + TypeScript + Vitest being set up

## Rollup on exit

At phase exit, run:
```bash
pnpm spec:rollup --phase 0
```

This scaffolds `phase-0-current.md` (see `docs/specs/mvp/templates/phase-rollup.template.md`). Engineering lead completes manual sections; reviewer approves; next phase can start.

## Reading order for Claude Code

When picking up a Phase 0 task:

1. This README (you are here)
2. `tasks.md` (generated by Spec Kit CLI) — find target task
3. `docs/specs/mvp/PRD.md` §F-001 through §F-006 for scope
4. `docs/specs/mvp/PRD.md` §6.4 (tech stack) + §6.5 (project structure map)
5. `CLAUDE.md` §2 (tech stack) + §4 (file structure rules)
6. `docs/specs/mvp/constitution.md` R1-R21 (all rules)

Do NOT load:
- Architecture specs (§01-§36) — not needed for setup
- Other phase files (future phases)
- `tasks-v2.md` in full (use for spot-lookup only)
