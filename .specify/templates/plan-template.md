---
# R17 Lifecycle Frontmatter — auto-included on every Neural-generated plan.
# Spec Kit MUST populate these fields. Do not remove the frontmatter block.

title: Implementation Plan — [FEATURE]
artifact_type: plan
status: draft                    # draft → validated → approved
version: 0.1
created: [DATE]
updated: [DATE]
owner: [OWNER]
authors: []
reviewers: []

supersedes: null
supersededBy: null

derived_from:
  - specs/[###-feature-name]/spec.md (the spec this plan implements)
  - docs/specs/mvp/architecture.md §6.4 (tech stack — no overrides allowed)
  - docs/specs/mvp/architecture.md §6.5 (project structure — file decisions)
  - docs/specs/mvp/constitution.md (R1-R23)
  - docs/specs/mvp/testing-strategy.md (§9.6 conformance pattern)
  - docs/specs/mvp/risks.md (§15 risk register + fallback protocols)

req_ids: []

impact_analysis: null            # Required if shared contract touched (R20)
breaking: false
affected_contracts: []

delta:
  new: []
  changed: []
  impacted: []
  unchanged: []

governing_rules:
  - Constitution R9 (Loose Coupling / Adapter Pattern)
  - Constitution R11 (Spec-Driven Development Discipline)
  - Constitution R17 (Lifecycle)
  - Constitution R20 (Impact Analysis Before Cross-Cutting Changes)
---

# Implementation Plan: [FEATURE]

> **Summary (~100 tokens):** [How the feature will be built — primary technical approach, key components touched, adapter changes if any, expected risk level. Generated by `/speckit.plan`.]

**Branch:** `[###-feature-name]`
**Date:** [DATE]
**Spec:** `specs/[###-feature-name]/spec.md`

---

## Summary

[Extract from spec.md: primary requirement + technical approach. 2-4 sentences.]

---

## Technical Context

**This is NOT free-form.** All values below are pinned by `docs/specs/mvp/architecture.md` §6.4. Do not propose alternatives. If the feature genuinely requires a new dependency or framework, the request goes through ASK FIRST (R11.3) BEFORE this plan is approved.

| Field | Value | Source |
|---|---|---|
| Language | TypeScript 5.x | architecture.md §6.4 |
| Runtime | Node.js 22 LTS | architecture.md §6.4 |
| Monorepo | Turborepo 2.x + pnpm 9.x | architecture.md §6.4 |
| Validation | Zod 3.x | architecture.md §6.4 + R2.2 |
| Browser | Playwright (default; stealth deferred to v1.1) | architecture.md §6.4 |
| Orchestration | LangGraph.js | architecture.md §6.4 |
| MCP | `@modelcontextprotocol/sdk` | architecture.md §6.4 |
| Primary LLM | Claude Sonnet 4 | architecture.md §6.4 + R10 |
| Fallback LLM | Deferred to v1.2 | PRD §3.2 |
| Database | Postgres 16 + pgvector | architecture.md §6.4 |
| ORM | Drizzle | architecture.md §6.4 + R7.1 |
| Cache / Queue | Redis (Upstash prod, Valkey dev) + BullMQ | architecture.md §6.4 |
| API framework | Hono 4.x | architecture.md §6.4 |
| Frontend | Next.js 15 App Router + shadcn/ui + Tailwind | architecture.md §6.4 |
| Auth | Clerk | architecture.md §6.4 |
| Storage | Cloudflare R2 (LocalDisk fallback in dev) | architecture.md §6.4 + §7.5 |
| Image annotation | Sharp | architecture.md §6.4 |
| PDF | Playwright `page.pdf()` | architecture.md §6.4 |
| Logging | Pino (JSON structured) | architecture.md §6.4 + R10.6 |
| Email | Resend (Mailpit in dev) | architecture.md §6.4 |
| Testing | Vitest unit + Playwright Test integration | architecture.md §6.4 + R3 |
| Deployment | Fly.io API + Vercel dashboard | architecture.md §6.4 |

**Performance / Scale targets:** see PRD §5 NF-001..NF-010. Reference specific NF-IDs in the Constraints field below.

**Project Type:** [single-project / web-app / library — must match architecture.md §6.5 layout, NOT introduce a new top-level structure]

**Constraints (cite PRD NF-IDs):** [e.g., NF-001 audit < 30 min, NF-002 ≤ $0.50/page, NF-003 grounding rejection 5-20%, NF-006 reproducibility ≥ 90% overlap]

---

## Constitution Check (GATE — must pass before Phase 0 research)

For each NEVER rule in PRD §10.3 + key ALWAYS rules, confirm this plan does not violate. If any check fails, the plan is REJECTED — return to spec or ASK FIRST.

- [ ] R5.3 + GR-007: No conversion-rate predictions in any output
- [ ] R6: Heuristic content boundary preserved — never in observability infrastructure
- [ ] R7.1: All DB access via Drizzle (no raw SQL except migrations + RLS policies)
- [ ] R7.2: RLS enabled on all client-scoped tables; `SET LOCAL app.client_id` in every transaction
- [ ] R7.4: No UPDATE/DELETE on append-only tables (`audit_log`, `rejected_findings`, `finding_edits`, `llm_call_log`, `audit_events`)
- [ ] R9: All external deps through adapters — `LLMAdapter`, `StorageAdapter`, `ScreenshotStorage`, `BrowserEngine`, `JobScheduler`, `EventBus`, `HeuristicLoader`, `AuthProvider`, `NotificationAdapter`, `DiscoveryStrategy`, `StreamEmitter`
- [ ] R10: TemperatureGuard enforces temperature=0 at adapter boundary on `evaluate`/`self_critique`/`evaluate_interactive`
- [ ] R10.1-R10.6: Files < 300 lines, functions < 50 lines, named exports, WHY-not-WHAT comments, no commented-out code, no `console.log`
- [ ] R11.2: Every implementation decision traces to a REQ-ID
- [ ] R20: If this plan modifies a shared contract (`AnalyzePerception`, `PageStateModel`, `AuditState`, `Finding`, any adapter interface, DB schema, MCP tool interface, grounding rule interface), an `impact.md` MUST be authored and approved BEFORE this plan
- [ ] R23: Kill criteria defined for any phase task > 2 hrs

Re-check after Phase 1 design. Any failed check → reject plan + return to spec.

---

## Project Structure

### Documentation (this feature)

```text
specs/[###-feature-name]/
├── spec.md                # /speckit.specify output (required prerequisite)
├── plan.md                # this file (/speckit.plan output)
├── research.md            # Phase 0 output — resolves NEEDS CLARIFICATION markers
├── data-model.md          # Phase 1 output (if Zod schemas added)
├── quickstart.md          # Phase 1 output (developer onboarding)
├── contracts/             # Phase 1 output (interface definitions)
└── tasks.md               # /speckit.tasks output (NOT created by /speckit.plan)
```

If this feature touches a shared contract, also produce:

```text
specs/[###-feature-name]/
└── impact.md              # required by R20 — see docs/specs/mvp/templates/impact.template.md
```

### Source Code (per architecture.md §6.5 — do not deviate)

The file structure is fixed by `architecture.md` §6.5. Refer to that section's "Where things go" decision tree. Reference the specific paths for THIS feature:

```text
# Per architecture.md §6.5 — canonical layout
packages/agent-core/src/
├── orchestration/         # AuditState, AuditGraph, nodes
├── gateway/               # AuditRequest + GatewayService
├── reproducibility/       # Snapshot + TemperatureGuard
├── browser-runtime/       # BrowserManager, OverlayDismisser, Rate/Circuit
├── perception/            # PageStateModel extractors
├── mcp/                   # MCP server + tools
├── safety/                # ActionClassifier, DomainPolicy
├── verification/          # ActionContract, strategies, VerifyEngine
├── analysis/              # nodes, grounding/rules, scoring, heuristics, personas, cross-page, quality
├── storage/               # AccessModeMiddleware, TwoStore
├── review/                # WarmupManager
├── delivery/              # ExecSummary, ActionPlan, ReportGenerator
├── observability/         # Pino logger, EventEmitter
├── adapters/              # ALL external deps wrapped here (R9)
├── db/                    # Drizzle schema + migrations
└── types/                 # cross-cutting Zod schemas + TS types

apps/cli/                  # CLI entry point (`pnpm cro:audit`)
apps/dashboard/            # Next.js 15 consultant dashboard
heuristics-repo/           # 30 heuristics JSON (private, R6)
tests/acceptance/          # End-to-end acceptance tests (Playwright Test)
```

**Files this feature creates / modifies:**

| File path | Layer | Purpose | New or existing? |
|---|---|---|---|
| `packages/agent-core/src/<dir>/<File>.ts` | <layer> | <purpose> | new / modified |

**Structure Decision:** Confirm this feature fits within the architecture.md §6.5 structure. If a new directory or top-level location is needed, that's a §6.5 amendment via ASK FIRST (R11.3) — not a plan change.

---

## Phase 0 — Research (if needed)

For each NEEDS CLARIFICATION marker in the spec or technical context:
- Use Context7 MCP for library docs (TypeScript, Playwright, LangGraph, Zod, Drizzle, Clerk, etc.)
- Reference `docs/specs/final-architecture/§NN-<name>.md` for Neural-specific design decisions
- Document findings in `research.md` with citations

Output: `research.md` with all NEEDS CLARIFICATION resolved.

---

## Phase 1 — Design

For each entity:
- Define Zod schema FIRST (R2.1, R2.2). TypeScript types via `z.infer`.
- Schemas go in `packages/agent-core/src/<module>/types.ts` adjacent to the module that owns them.
- If schema is a shared contract (e.g., `AnalyzePerception`, `Finding`, adapter interface) → `impact.md` required (R20).

For each external dependency:
- Adapter interface in `packages/agent-core/src/adapters/<Name>Adapter.ts`
- Concrete implementation in same file or sibling
- Mock implementation for tests (R3)

For each user story:
- Quickstart steps in `quickstart.md` (developer onboarding)
- Conformance test stub in `packages/agent-core/tests/conformance/<component>.test.ts` (PRD §9.6)

Output: `data-model.md` + `quickstart.md` + `contracts/` + `impact.md` (if shared contract touched).

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified.**

Per Constitution R22.2 (Ratchet), every justified violation needs a `why:` provenance block.

| Violation | Why Needed | Simpler Alternative Rejected Because | Provenance |
|-----------|------------|--------------------------------------|------------|
| [e.g., 4th adapter category] | [need] | [why existing adapters insufficient] | [source / commit / research file] |

If no violations: write "None — plan respects all 23 Constitution rules."

---

## Approval Gates

| Gate | Approver | Evidence |
|---|---|---|
| Spec → Plan transition | spec author + product owner | `spec.md status: approved` |
| Tech stack adherence | engineering lead | All §6.4 fields match canonical |
| Constitution check | engineering lead | All checkboxes ticked OR variances justified in Complexity Tracking with provenance |
| Impact analysis (if applicable) | engineering lead | `impact.md status: approved` |
| Plan → Tasks transition | engineering lead | This plan `status: approved` |

After all gates pass, run `/speckit.tasks` to decompose into `tasks.md`.
