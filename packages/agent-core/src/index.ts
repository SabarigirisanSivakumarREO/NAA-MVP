/**
 * @neural/agent-core — barrel export.
 *
 * Phase 0: empty surface — modules land in subsequent phases.
 * Sub-paths (e.g., `@neural/agent-core/observability`) are exposed via
 * package.json#exports rather than re-exported here, to keep the root
 * surface narrow until adapters / domain modules land.
 */
export {};
