/**
 * Shim — re-exports `prioritizeHeuristics` from `./filter.js`.
 * Conformance test resolves `prioritize.ts`; spec canonicalises `filter.ts`.
 */
export { prioritizeHeuristics } from './filter.js';
